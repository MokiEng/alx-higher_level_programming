// Attach a click event handler to the <div> element with id "red_header"
$('DIV#red_header').click(function () {
  // Update the text color of the <header> element to red
  $('HEADER').css('color', '#FF0000');
});
