-- a script that creates a table called first_table
-- +in the current database in your MySQL server.
-- with description of a table's id and name.
CREATE TABLE IF NOT EXISTS first_table (
	id INT PRIMARY KEY,
	name VARCHAR(256));
